// import api from './api';

const resourceService = {
  getResourceUsage: async () => {
    // Mock API response (replace with real data later)
    return { power: 1500, water: 2000 };
  },
};

export default resourceService;